# netacc

基于 go-libp2p 的流级带宽聚合库：把一个对等点之间的多条网络路径叠加成一条高带宽逻辑流。

## Language

### 核心概念

**聚合流 (Aggregated Stream)**:
本库向应用暴露的一条逻辑流，呈现普通可靠有序字节流语义，内部数据被拆分并经多条路径传输。
_Avoid_: 多径连接、聚合连接、bonding

**路径 (Path)**:
承载聚合流数据的一条通道，对调度器而言各路径完全同构。一条路径是一条已建立连接的子流，可动态增删。
_Avoid_: 链路、通道（channel）、sub-connection

**直连路径 (Direct Path)**:
不经中转、直接跑在某个传输上的路径（同一传输开多条连接也算多条路径）。
_Avoid_: 多链路直连

**中继路径 (Relayed Path)**:
经中继节点转发建立的路径（A→C→B），对聚合层呈现为一条普通的端到端加密连接。
_Avoid_: 中转路径、跳板路径

**传输 (Transport)**:
直连路径底层使用的网络协议，本库范围内指 TCP、QUIC、WebSocket、WebTransport、WebRTC。

**中继节点 (Relay)**:
运行本库中继组件的 libp2p 节点，负责为其他对等点转发大流量；受 peerID 白名单和公平带宽分配约束。中继的接入信息由应用通过自己的渠道获知，不属于本库职责。
_Avoid_: relay server、中转服务器

**路径描述符 (Path Descriptor)**:
描述一条待建路径的信息单元，由聚合协议的控制消息携带：直连路径为传输偏好与目标 multiaddr，中继路径为中继节点 peerID 与逐跳传输偏好。
_Avoid_: 路径配置

### 内部机制

**调度器 (Scheduler)**:
发送端决定每个数据帧走哪条路径的组件，默认策略为吞吐优先的加权条带（按各路径实测带宽比例分配、RTT 感知）。
_Avoid_: 负载均衡器、多径调度

**重排缓冲 (Reorder Buffer)**:
接收端把各路径乱序到达的带全局序号帧拼回有序字节流的缓冲。

**公平带宽分配 (Fair Sharing)**:
中继侧的弹性限速策略：空闲带宽可被单用户用满，新用户加入时占用大者降速，最大化利用且防独占（max-min fairness 语义）。
_Avoid_: 固定限速、per-user cap
